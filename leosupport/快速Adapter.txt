import android.content.Context;

public class ${NAME} extends BaseRecyclerAdapter<${Model}, ${NAME}.MainHolder> {

    public ${NAME}(Context context, List<${Model}> mList) {
        super(context, mList);
    }

    @Override
    protected int setLayout() {
        return R.layout.item_;
    }

    @Override
    protected RecyclerView.ViewHolder setViewHolder(View itemView) {
        return new MainHolder(itemView);
    }

    @Override
    protected void initView(MainHolder holder, int position, ${Model} mode) {

    }

    @Override
    protected void initListener(MainHolder holder, int position, ${Model} mode) {

    }

    static class MainHolder extends ${NAME}.ViewHolder {

        public MainHolder(View itemView) {
            super(itemView);
            ButterKnife.bind(this, itemView);
        }
    }
}
